<table>
  <ul>
    <% @artists.each do |a| %>
    <li>
      <%= link_to a.name, a %>
    </li>
      <% a.songs.each do |s| %>
      <li>
        <%= link_to "#{s.artist_name} - #{s.title}", s %>
      </li>
      <% end %>
    <% end %>
  </ul>
</table>
